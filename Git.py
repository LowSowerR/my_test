"""
git --help
git
dir - Показывает дериктории
cd - погружение в дериктории(можно перенести папку в консоль
git init - создает в проекте git
cd. - Остаться на уровне
cd .. - Погрузится на уровень(1 точка - +1 уровень начиная с 2) выше
git status
git add <name_file> - добавляет файлы, которые будут отслеживаться
git rm --cached <name_file>
git branch - вывод всех веток проекта
git branch <название новой ветки> - Создание новой ветки
git commit -m <название сохр> - сохранение резервной копии проекта
git checkout <название ветки> - смена ветки проекта
git checkout -b <Название ветки> - создание и погружение в ветку
git branch -D <название ветки> - удаление ветки
git branch -M main Переименновывает основную ветку в main
git merge <Название ветки> - обьединение 2ух веток (в той которой погружены + названную
git config --global user.email - добавить свой email
"""
print("Hellow world")

for index in range(1,100):
    print(index)

for number in [1, 2, 3, 4, 5, 6]:
    print(number)

for number in [1, 2, 3, 4, 5, 6]:
    print(number)

for number in [1, 2, 3, 4, 5, 6]:
    print(number)

for number in [1, 2, 3, 4, 5, 6]:
    print(number)
